{
  "base.schema.json": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://sparkfabrik.com/schemas/github-ci-assembler/base.schema.json",
    "title": "GitHub CI Assembler Configuration",
    "description": "Defines the pipeline skeleton and workflow root properties from configuration.yml.",
    "type": "object",
    "required": [
      "version",
      "stages"
    ],
    "additionalProperties": false,
    "properties": {
      "version": {
        "type": "string",
        "const": "1",
        "description": "Schema version. Currently only '1' is supported."
      },
      "name": {
        "type": "string",
        "description": "Workflow display name.",
        "minLength": 1
      },
      "on": {
        "type": "object",
        "description": "Workflow triggers. Must use map form only (no scalar or list shorthand).",
        "additionalProperties": true
      },
      "defaults": {
        "type": "object",
        "description": "Workflow-level defaults (e.g., defaults.run.shell).",
        "additionalProperties": true
      },
      "env": {
        "type": "object",
        "description": "Workflow-level environment variables emitted at root env in the generated workflow.",
        "additionalProperties": true
      },
      "permissions": {
        "type": "object",
        "description": "Base workflow permissions map, deep-merged with package/project permissions overlays.",
        "additionalProperties": true
      },
      "stages": {
        "type": "array",
        "description": "Ordered list of stage names defining the pipeline topology.",
        "items": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_-]*$"
        },
        "minItems": 1,
        "uniqueItems": true
      }
    }
  },
  "package.schema.json": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://sparkfabrik.com/schemas/github-ci-assembler/package.schema.json",
    "title": "GitHub CI Assembler Package Configuration",
    "description": "A package contributing jobs to pipeline stages, optional file-scoped env defaults for package jobs, and an optional workflow permissions overlay.",
    "type": "object",
    "required": [
      "id",
      "hooks"
    ],
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9_-]*$",
        "description": "Unique package identifier. Used as the job id prefix and as the provided_by target in project.yml. Must be unique across all packages.",
        "minLength": 1,
        "maxLength": 50
      },
      "env": {
        "type": "object",
        "description": "File-scoped env defaults merged into each job in this package (job env wins on conflict).",
        "additionalProperties": true
      },
      "permissions": {
        "type": "object",
        "description": "Workflow permissions overlay deep-merged into the root workflow permissions map.",
        "additionalProperties": true
      },
      "hooks": {
        "type": "object",
        "description": "Map of stage name → jobs. Stage names are validated against configuration.yml at assembly time.",
        "minProperties": 1,
        "patternProperties": {
          "^[a-z0-9][a-z0-9_-]*$": {
            "type": "object",
            "description": "Jobs for this stage. Keys are job ids, values are GitHub Actions job definitions (opaque).",
            "minProperties": 1,
            "patternProperties": {
              "^[a-z0-9][a-z0-9_-]*$": {
                "type": "object",
                "description": "GitHub Actions job definition. Passthrough — not validated by this schema."
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    }
  },
  "project.schema.json": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://sparkfabrik.com/schemas/github-ci-assembler/project.schema.json",
    "title": "GitHub CI Assembler Project Configuration",
    "description": "Per-project customizations with file-scoped env defaults, an optional workflow permissions overlay, and job operations (extend, replace, disable, new).",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "env": {
        "type": "object",
        "description": "File-scoped env defaults merged into each project-defined job (new/extend/replace), with lower priority than each job's own env.",
        "additionalProperties": true
      },
      "permissions": {
        "type": "object",
        "description": "Workflow permissions overlay deep-merged into the root workflow permissions map with highest priority.",
        "additionalProperties": true
      },
      "hooks": {
        "type": "object",
        "description": "Map of stage name → jobs.",
        "minProperties": 1,
        "patternProperties": {
          "^[a-z0-9][a-z0-9_-]*$": {
            "type": "object",
            "description": "Jobs for this stage. Each job is one of: new (no directive), extend, replace, or disable.",
            "minProperties": 1,
            "patternProperties": {
              "^[a-z0-9][a-z0-9_-]*$": {
                "$ref": "#/$defs/projectJob"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    },
    "$defs": {
      "providedByRef": {
        "type": "object",
        "required": [
          "provided_by"
        ],
        "additionalProperties": false,
        "properties": {
          "provided_by": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "The id of the package that provides the target job.",
            "minLength": 1
          }
        }
      },
      "projectJob": {
        "type": "object",
        "description": "A project job. Must be exactly one of: extend (has 'extend' key), replace (has 'replace' key), disable (has 'disable' key), or new (none of the above). Multiple directives on the same job are not allowed.",
        "oneOf": [
          {
            "$ref": "#/$defs/extendJob"
          },
          {
            "$ref": "#/$defs/replaceJob"
          },
          {
            "$ref": "#/$defs/disableJob"
          },
          {
            "$ref": "#/$defs/newJob"
          }
        ]
      },
      "extendJob": {
        "type": "object",
        "description": "Deep-merges project properties into an existing package job. Properties not declared here are preserved from the package.",
        "required": [
          "extend"
        ],
        "properties": {
          "extend": {
            "$ref": "#/$defs/providedByRef"
          }
        },
        "not": {
          "anyOf": [
            {
              "required": [
                "replace"
              ]
            },
            {
              "required": [
                "disable"
              ]
            }
          ]
        },
        "additionalProperties": true
      },
      "replaceJob": {
        "type": "object",
        "description": "Fully substitutes an existing package job. Nothing from the original survives.",
        "required": [
          "replace"
        ],
        "properties": {
          "replace": {
            "$ref": "#/$defs/providedByRef"
          }
        },
        "not": {
          "anyOf": [
            {
              "required": [
                "extend"
              ]
            },
            {
              "required": [
                "disable"
              ]
            }
          ]
        },
        "additionalProperties": true
      },
      "disableJob": {
        "type": "object",
        "description": "Removes a package-provided job from the pipeline.",
        "required": [
          "disable"
        ],
        "properties": {
          "disable": {
            "$ref": "#/$defs/providedByRef"
          }
        },
        "not": {
          "anyOf": [
            {
              "required": [
                "extend"
              ]
            },
            {
              "required": [
                "replace"
              ]
            }
          ]
        },
        "additionalProperties": false
      },
      "newJob": {
        "type": "object",
        "description": "A new job contributed by the project. Not prefixed in output.",
        "not": {
          "anyOf": [
            {
              "required": [
                "extend"
              ]
            },
            {
              "required": [
                "replace"
              ]
            },
            {
              "required": [
                "disable"
              ]
            }
          ]
        },
        "additionalProperties": true
      }
    }
  }
}
