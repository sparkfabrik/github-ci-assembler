Read @spec/gh-ci-assembler.md

I would like you to analyze the specification and let me know if they are consistent and clear and implementable.

I also would like to apply the following changes to the specification and implementation, So I would also like you to let me know if these changes are consistent and clear and implementable.

- Both packages file and the project file should not allow the following three keywords.

  - on
  - defaults
  - name

That should only be allowable in the configuration.yaml file (like before, they follow standard GitHub actions syntax).

The syntax of the project and package files receives a new root level keyword
argument called env which defines a map of environment variables that the
assembler should merge into the `env` map of the jobs defined in the current file (pkg or project), with priority lower
than the actual job's env map, it's like a file specific, global environment definition.

The current specification say that the needs key in a job definition will be merged in, as is, with the computed needs in the output file.
Now we want to change the specs to allow in that needs array value (in a
"source" job definition) only job ids in the same stage and in the same current
file with a non-prefixed job id value.

Also we considered that the virtual stages feature is too magic. We want to
remove that feature and leverage the fact that pre and post stages will be
written explicitly in the configuration YAML file manually.

I reviewed specs/gh-ci-assembler.md and the current code (internal/config,
internal/assembly, internal/validation): the spec is mostly implementable, but
not fully aligned with implementation (notably section 7 documents
validate/--diff, while the CLI currently exposes only generate) and §5.3 needs
behavior is still ambiguous for project-defined jobs. Your proposed changes are
coherent overall and implementable, but they need two explicit decisions to be
fully clear: where workflow-level env should live after repurposing pkg/project
env (move to configuration.yml or remove), and whether project extend/replace
jobs may reference package jobs in source needs or only same-stage jobs
declared in project.yml. Once those are fixed, the implementation delta is
straightforward: move root name/on/defaults ownership to configuration.yml,
treat pkg/project root env as file-scoped job-env defaults (job-level env
wins), validate/resolve needs as same-stage/same-file/non-prefixed IDs, and
remove virtual-stage expansion so only explicitly listed stages are valid.

---

Let's try to address your your points.

the validate/--diff is on hold for the moment, Feel free to update the specification to avoid further future confusion.

workflow-level env: so basically I'm expecting that in the compiled output
workflow file there should be no root level env since the source file root
level keyword env exists only for the purpose to merge its definition into
the jobs of the current file.

project extend/replace jobs: the idea is that the algorithm / approach for the
project file is the same as the package files, meaning that they can reference
only same-stage jobs declared in project.yml.
The rationale is that the needs keyword forces a specific sequence of
operations so if a project wants to "need" / depend on a package job than it
can simply place a job in the "post-X" stage (assuming X is the stage of the
job it wants to depend on)

Did I understand and addressed your question? Does this clarification reduce the ambiguity? If not, let me know.
