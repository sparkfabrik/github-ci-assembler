
Look at the output of the command

git diff specs/gh-ci-assembler.md

It shows that I removed the constraint that
the existing needs in source files (project or package) must reference
non-prefixed job IDs in the same stage and package stage.

The desiderata is that whatever is present in the source files will be merged with the automatic dependencies.
I want you to check if the spec is consistent in this constraint removal and update the implementation.

