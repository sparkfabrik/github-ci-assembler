version: "1"

name: GitHub CI Assembler

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

defaults:
  run:
    shell: bash

permissions:
  actions: read
  contents: read

stages:
  - build
  - notify
  - test
  - deploy
