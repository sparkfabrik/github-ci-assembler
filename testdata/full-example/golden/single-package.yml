# AUTO-GENERATED by gh-ci-assembler — do not edit manually
# Contributing files:
#   -  config: ../../testdata/full-example/configuration.yml
#   - package: ../../testdata/full-example/pkg_base.yml

name: GitHub CI Assembler
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - develop
defaults:
  run:
    shell: bash
env:
  CI_ORCHESTRATOR: gh-ci-assembler
  GLOBAL_TIMEOUT: "600"
permissions:
  actions: read
  checks: write
  contents: read
jobs:
  #  ── Stage: build ─────────────────────────────────────
  build--base--placeholder:
    name: placeholder - base [build]
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Base package placeholder"
