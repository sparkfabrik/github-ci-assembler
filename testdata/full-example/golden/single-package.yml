# ┌──────────────────────────────────────────────────────────────────────┐
# │ AUTO-GENERATED by gh-ci-assembler — do not edit manually             │
# │ Source: configuration.yml, pkg_base.yml                               │
# │ Generated: 2025-01-01T00:00:00Z                                       │
# └──────────────────────────────────────────────────────────────────────┘

name: GitHub CI Assembler
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - develop
defaults:
  run:
    shell: bash
env:
  CI_ORCHESTRATOR: gh-ci-assembler
  GLOBAL_TIMEOUT: "600"
permissions:
  actions: read
  checks: write
  contents: read
jobs:
  #  ── Stage: build ─────────────────────────────────────
  build--base--placeholder:
    name: placeholder - base [build]
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Base package placeholder"
